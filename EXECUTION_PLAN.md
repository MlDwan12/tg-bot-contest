# План исполнения — перестройка проекта

> Сводит воедино [ARCHITECTURE_REVIEW.md](./ARCHITECTURE_REVIEW.md) (что не так + приоритеты) и
> [PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md) (целевая раскладка).
> **Подход:** эволюционно (strangler), НЕ rewrite. Каждый шаг = 1 или несколько коммитов, компилируется и проходит тесты; откат = ревёрт коммита.
> **Прогресс пишем в [PROGRESS_LOG.md](./PROGRESS_LOG.md).**

**Принципы исполнения:**
- Ничего не менять на горячих путях без тестов (Фаза 0 — предусловие всего).
- Один шаг — маленький изолированный коммит с понятным «done».
- Прод live с деньгами → сначала корректность/безопасность (Фазы 1–3), потом чистота.
- Порядок фаз можно нарушать только для «быстрых побед» (Фаза 7) — они низкорисковые, но всё равно после Фазы 0.

**Легенда приоритета:** 🔴 сейчас · 🟡 скоро · 🟢 потом.

---

## Фаза 0 — ⓿ Страховочная сеть (предусловие) 🔴
**Цель:** покрыть тестами текущее поведение горячих путей ДО любых правок.
- 0.1 Поднять интеграционный харнесс: реальные Postgres + Redis (docker/testcontainers), отдельный тест-конфиг, чистка БД между тестами.
- 0.2 Характеризационные тесты (фиксируют КАК ЕСТЬ сейчас):
  - **участие:** дубль (уникальный индекс + `23505` → идемпотентный ответ), проверка `requiredChannels`, гейты статуса;
  - **завершение:** идемпотентность `finishContestIdempotent`, advisory lock, гонка F1 (воспроизвести!);
  - **розыгрыш:** случайный выбор, MANUAL с предвыбранным победителем (reuse-guard).
- **Тест:** всё зелёное против реального PG/Redis.
- **Откат:** не нужен (только добавление).
- **Done:** три потока покрыты, CI/локально гоняется.

## Фаза 1 — initData-аутентификация участия 🔴🔴 (живая дыра с деньгами)
**Цель:** сделать `telegramId` недоказуемым снаружи → закрыть накрутку/риггинг.
- 1.1 Утилита валидации Telegram `initData` (HMAC-SHA256 по токену бота, проверка `auth_date`/срока).
- 1.2 Guard/pipe: извлекает проверенный `telegramId` из `initData`, **тело игнорируется**. Навесить на `POST /contest/:id/participate`.
- 1.3 **Контракт с фронтом:** Mini App шлёт `initData` (заголовок/поле). ⚠️ Согласовать с фронтенд-разработчиком — межкомпонентная зависимость.
- 1.4 Тесты: валидный `initData` → 200; поддельный/отсутствующий → 401; повтор в пределах окна → повторное участие только собой (блок уникальным индексом).
- **Откат:** ревёрт; на время выката можно за фиче-флагом принять оба пути.
- **Done:** без валидной подписи участие отклоняется; тесты зелёные.

## Фаза 2 — Добить гонку F1 при завершении 🔴
**Цель:** гарантировать один розыгрыш на конкурс при любом параллелизме.
- 2.1 В `completeContest` — атомарные ворота ПЕРЕД розыгрышем: `updateStatusIfNotCompleted` (или advisory lock, как в шедулере). Разыгрывает только тот, кто выиграл CAS.
- 2.2 Свести `completeContest` и `finishContestIdempotent` к одному пути финализации.
- 2.3 Тесты: два параллельных `completeContest` → один набор победителей; ручное впритык к шедулеру → один розыгрыш.
- **Откат:** ревёрт коммита.
- **Done:** конкуррентный тест доказывает единственный розыгрыш.

## Фаза 3 — Аудит (🔴, прямая цель пользователя)
**Цель:** доказуемость исхода на призах до 10М ₽.
- 3.1 Миграция: таблица аудита (`contestId`, `action`, `actorOperatorId`, `winnerIds`, `strategy`, `rngSeed`, `createdAt`).
- 3.2 MANUAL: назначение победителя пишет запись аудита (кто/когда).
- 3.3 RANDOM: перейти на `crypto.randomInt`; сохранять seed/входные данные розыгрыша.
- 3.4 Тесты: назначение пишет аудит; розыгрыш воспроизводим из seed.
- **Откат:** ревёрт + миграция down.

## Фаза 4 — Sentry довести до ума 🟡 (наблюдаемость = set-and-forget)
- 4.1 `Sentry.init` первым в bootstrap; отправка ошибок из `AllExceptionsFilter`; удалить мёртвый `@sentry/tracing`.
- 4.2 Проверить, что необработанные ошибки реально долетают.
- **Done:** тестовая ошибка видна в Sentry.

## Фаза 5 — Консолидация bcrypt 🟡
- 5.1 Оставить `bcryptjs`; заменить нативный `bcrypt` в `auth.service`; проверить, что существующие хэши верифицируются.
- 5.2 Удалить зависимость `bcrypt`.

## Фаза 6 — `infra/telegram` листовой адаптер + разрыв циклов 🟡 (S6/Q3.1/Q3.4/Q4.4)
**Цель:** один транспорт, одна точка rate-limit, ноль `forwardRef`.
- 6.1 Вынести `TelegramService` → `infra/telegram/` + порт `ITelegramTransport`; единый `bottleneck` здесь; очередь `telegram-messages` воскресить как воронку ИЛИ удалить.
- 6.2 Перенаправить всех потребителей; убрать `forwardRef`, где раскрылось.
- 6.3 Тесты: транспорт фейкается через порт.
- **Откат:** ревёрт (крупный шаг — дробить на под-коммиты).

## Фаза 7 — Быстрые победы 🟢 (низкий риск, можно рано, но после Фазы 0)
- 7.1 Удалить мёртвое: `mongoose`, `@nestjs/mongoose`, `@sentry/tracing`.
- 7.2 `.tar` + `dist/` из git (`.gitignore`).
- 7.3 Конфиг в env (CORS, порт); порядок `helmet` до роутеров; явный `jsonwebtoken` в deps.

## Фаза 8 — Консолидация структуры 🟢 (S1/S2/S3/S5)
- 8.1 Слить `common` + `shared` + `*/commons` → один `common/`.
- 8.2 Единая конвенция имён `kebab-case.<type>.ts`.
- 8.3 Убрать barrels в кросс-DI модулях.
- 8.4 Удалить дубли `shared/types` (зеркало dto).

## Фаза 9 — Схлопнуть CQRS-репозитории 🟢 (S4, оппортунистически)
- Один `*.repository.ts` на агрегат, по мере касания модуля.

## Фаза 10 — Дробление толстых сервисов 🟢
- `participate()` → цепочка правил участия.

## Фаза 11 — Эффективность F2/F4 🟢 (низкий приоритет, нагрузка не растёт)
- F2: проверку членства с горячего пути (кэш/асинхронно). F4: розыгрыш `ORDER BY random() LIMIT n`.

## Фаза 12 — Опц. терминальное состояние брошенного конкурса 🟢 (продуктовое)
- Потолок продлений + backoff оповещений (на усмотрение).

---

## Карта зависимостей между фазами
- **0 обязательна перед всем.**
- 1, 2, 3 — независимы друг от друга, делать в порядке приоритета (деньги).
- 6 не блокирует 1–3; 4,5,7 независимы, можно вставлять между.
- 8, 9, 10 — структурные, после того как корректность закрыта; 9 идёт естественно вместе с касанием модулей.

## Текущий шаг
См. **[PROGRESS_LOG.md](./PROGRESS_LOG.md)** — там всегда актуальное «где мы и что дальше».
